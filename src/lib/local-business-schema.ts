import { buildBusinessSchema } from "@/lib/schemas/business-schema";

/** Site-wide LocalBusiness / TravelAgency — injected in root layout */
export const localBusinessSchema = buildBusinessSchema();
